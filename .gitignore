# ignore sensitive files
.secrets
oauth2-proxy.cfg

# ignore sensitive files (TODO: remove)
gluetun/data/
radicale/configs/users
soulseek/slskd.yml
cloudflare-ddns/config.json
caddy/configs/snippets/common.conf

# ignore temporary files
*.swp

# ignore trashes
*.filen.trash.local/
*.recycle/
*.Trash-0/

# ignore intake (TODO: remove)
dawarich/imports/

# ignore backups
*.bak*
*/backups/
*.dump

# ignore extremely large folders (TODO: remove, migrate to disk)
dawarich/photon-db/

# ignore archived stacks
+deprecated/
*.7z*

# ignore internal repos
*.git/
*build/

# ignore data backed up elsewhere (TODO: remove and migrate)
*/data/

# ignore ephemeral folders/files
*ephemeral

# temporarily ignore certain apps (until I migrate to Docker volumes in LXCs) (TODO: remove)
backrest/
firefox-sync/
headscale/
