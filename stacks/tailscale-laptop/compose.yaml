services:
  tailscale:
    image: tailscale/tailscale:latest
    container_name: tailscale-laptop
    privileged: true
    cpus: 2.0
    network_mode: host
    env_file:
      - .env
      - .secrets
    volumes:
      - tailscale-laptop_data:/var/lib/tailscale
      - /dev/net/tun:/dev/net/tun
    cap_add:
      - net_admin
      - sys_module
    restart: always
volumes:
  tailscale-laptop_data:
    external: true
