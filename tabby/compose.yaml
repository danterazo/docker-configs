services:
  tabby:
    image: ghcr.io/tabbyml/tabby:latest
    container_name: tabby
    command: >
      serve
      --device cpu
    ports:
      - "8080:8080/tcp"
    volumes:
      - data:/data
    devices:
      - /dev/kfd
      - /dev/dri
    env_file: .env
    restart: unless-stopped

volumes:
  data:
