[project]
name = "docker"
version = "0.1.0"
description = ""
authors = [
    {name = "Dante Razo",email = "github.d2brf@simplelogin.fr"}
]
requires-python = ">=3.14"


[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry.dependencies]
ruff = "^0.16.7"
